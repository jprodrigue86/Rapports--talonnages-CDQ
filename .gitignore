# Private application source — never publish in this public PWA repository
AppsScript/
*.gs

# Local/private configuration
.env
.env.*
*.secret
*.secrets
secrets.*
credentials.*

# Backups that may contain private source
*.bak
*.backup
*.zip

# Local reconstruction of private Apps Script exports
.audit/

# Reproducible Android output; keep only the compressed public client snapshot.
balance-cdq-android/.gradle/
balance-cdq-android/app/build/
balance-cdq-android/build/
balance-cdq-android/web-source/Selector.html
