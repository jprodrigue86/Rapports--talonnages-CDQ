name: CDQ workflow test

on:
  push:
    paths:
      - '.github/workflows/cdq-fix-note-first-touch.yml'

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CDQ workflow OK"
