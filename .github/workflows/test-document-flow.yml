name: Document flow and guarded cleanup
on:
  push:
    paths:
      - 'bundles/balance-cdq/v25.10/**'
      - 'bundles/balance-cdq/v25.11/**'
      - 'tests/mobile-patch.test.mjs'
      - 'tests/mobile-compat.test.mjs'
      - 'tests/fixtures/**'
      - 'tests/helpers/**'
      - 'apps-script-manager/app.js'
      - 'tests/document-flow-cleanup.test.mjs'
  pull_request:
jobs:
  document-flow:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node --test tests/document-flow-cleanup.test.mjs tests/mobile-patch.test.mjs tests/mobile-compat.test.mjs
